import { Drawer as BaseDrawer } from '@base-ui/react/drawer';

/** Creates a handle that connects a `Drawer` to one or more detached `DrawerTrigger` elements. */
export const createDrawerHandle = BaseDrawer.createHandle;
