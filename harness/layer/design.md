# Design language

<!-- PLACEHOLDER. Authored by issue #9 from the buckets in
     docs/research/astryx-guidance-and-gaps.md (branch research/astryx-guidance).

     Write ONLY from buckets B (open choices), C (silences) and D (discovery).
     Bucket A is 27 opinions Astryx already states - restating them is wasted ink and
     will score at the ceiling in every Arm.

     The highest-value section is the chart boundary: there is no Chart component in
     core 0.3.0, Astryx's own templates use three incompatible chart stacks, and issue #4
     found every raw value in a real Run landing at exactly that seam. -->
