<!-- Plumbing smoke test only. Not a real Fixture - deliberately trivial so the pipeline
     can be verified without paying for a real Run. -->

Create the file `apps/web/src/routes/smoke.tsx` exporting a route at `/smoke` that renders the
single word "smoke". Do not install anything. Keep it minimal.
