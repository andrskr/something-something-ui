<!-- Persona: naive. EDIT fixture. The before-state in src/ is a REAL page generated by an
     unaided Run (Sonnet, Arm off) - flaws included, deliberately. It hand-rolls its chart in
     raw SVG and uses hex fallbacks inside token references.

     The interesting question is not whether the agent can add a metric. It is whether the agent
     MATCHES the existing flaw or FIXES it, and whether the Layer changes that answer. -->

The overview page at `/overview` is working well, thanks.

Two things to add. I also need to track average basket size week on week, alongside the numbers
already there. And I want a second chart showing how the sites break down by region, so I can see
whether a bad week is one region or all of them.

Keep it consistent with the rest of the page. It needs to build.
