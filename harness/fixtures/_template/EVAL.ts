// GRADER-ONLY. Never reaches the agent's workspace.
// Present for @vercel/agent-eval compatibility (ADR 0001) - our own runner does not
// execute it yet. Keep assertions here in sync with fixture.json's expectations.
export const evalPlaceholder = true;
