<!-- Arm 3 = Arm 2's block plus the pointer below. The pointer is the ENTIRE discovery
     surface for the Layer, so it has to earn the click. Discovery is measured as whether
     the Run actually read these files. See issue #7. -->

## Our design language

Read `design.md` before building any UI in this app. It carries our conventions for charts, tables,
number formatting, page structure and states, and it points at the reference docs in `references/`.
It covers decisions the component library deliberately leaves open.
