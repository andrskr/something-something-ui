<!-- Arm 2 and Arm 3 both receive this block. Keep it THIN.
     Only rules that must never be missed belong here. Everything else is Arm 3's
     discoverable material — if this block grows, it absorbs the effect and Arm 3
     has nothing left to show. See issue #7. -->

## Building UI in this app

- Numeric and currency table columns are right-aligned. Text and dates are left-aligned.
- Every data region has a loaded, an empty and an error state. None may be omitted.
